Chat Container
.chat-container {
    display: flex;
    flex-direction: column;
    width: 1000px; /* Set width */
    height: 500px; /* Set height */
    background-image: url("http://images5.fanpop.com/image/photos/30000000/Fight-Club-HD-fight-club-30031933-1280-534.jpg");
    background-size: cover;
    border: 1px solid #ddd; /* Optional: Add border to the chat box */
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.3); /* Optional: Add shadow for better visibility */
    margin: 0 auto; /* Center the chat box */
    position: relative; /* Ensure form sticks to the bottom */
    overflow: hidden; /* Ensure no overflow */
}

/* Messages */
#messages {
    list-style-type: none;
    padding: 0;
    margin: 0;
    flex: 1; /* Grow to fill available space */
    overflow-y: auto;
    padding-bottom: 50px; /* Add space for the form */
}

#messages li {
    padding: 8px;
    background: #eee;
    margin: 10px 50px;
    border-radius: 4px;
    
}

/* Form */
#form {
    display: flex;
    align-items: center;
    padding: 10px;
    background: white;
    border-top: 1px solid #ddd;
    width: 100%; /* Ensure full width */
    box-sizing: border-box; /* Ensure padding is included in width */
    position: absolute;
    bottom: 0; /* Stick to the bottom of the container */
}


#input {
    border: 1px solid #ccc;
    padding: 10px;
    width: 90%;
    border-radius: 4px;
    box-sizing: border-box; /* Ensure padding is included in width */
}

button {
    border: none;
    background: #007bff;
    color: white;
    padding: 10px;
    width: 10%;
    border-radius: 4px;
    cursor: pointer;
}

button:hover {
    background: #0056b3;
}
